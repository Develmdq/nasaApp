import { Header, Body } from '../components'
import { AppContainer } from "../styles/styledComponents";

const Home = () => {
  return (
    <AppContainer>
      <Header />
      <Body/>
    </AppContainer>
  )
}

export default Home;
